# require "student_commands"
describe StudentCommands do

  describe "Invalid Command" do
    context "The command is invalid" do
      it "should be recognized as invalid and return the help message" do
        expect 
      end


end
